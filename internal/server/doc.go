// Package server implements the HTTP tile endpoints: on-demand generation with
// bounded admission, MBTiles serving, per-client rate limiting, and the status
// stream used by the demo UI.
package server
