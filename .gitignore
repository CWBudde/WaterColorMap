# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
/watercolormap

# Test binary
*.test

# Output of the go coverage tool
*.out
coverage.html

# Go workspace file
go.work

# Generated tiles
tiles/*.png
tiles/**/*.png
# The per-tile source-data stamp store `generate` and `serve` write beside them
tiles/stamps.db
tiles/**/stamps.db
testdata/output/

# Generated test artifacts (use repo-root testdata/*)
internal/watercolor/testdata/output/
internal/watercolor/testdata/golden/

# Cache directories
cache/
.gocache/

# Downloaded datasets (OSM water polygons; `just fetch-water-polygons`)
data/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Config overrides (keep example configs)
config.yaml
local.yaml

# Build artifacts
dist/
# `go build ./cmd/wasm` writes its output binary here as `wasm`
/wasm
*.wasm
# Go's WASM runtime shim, copied out of GOROOT by scripts/copy-wasm-exec.sh
docs/wasm-playground/wasm_exec.js

# Profiling data
*.prof

#AI agents
.claude